 
symbols ok/0, ko/0, h/1, pair/2, fst/1, snd/1;

private a, b, c, u;

channels C;
var X, Y, Z;
rewrite fst(pair(X,Y)) -> X;
rewrite snd(pair(X,Y)) -> Y;


variants? pair(pair(fst(X),snd(X)),pair(fst(X),snd(Y))); 
