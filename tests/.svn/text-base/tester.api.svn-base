symbols enc/2, dec/2;

private a, b, k, ok;

var X, Y, Z;

rewrite dec(enc(X, Y), Y) -> X;

r = out(enc(a, k)).in(X).[enc(dec(X, k), k) = X].0;
s = out(enc(b, k)).in(X).[X = enc(b, k)].0;

equivalent? r and s;
