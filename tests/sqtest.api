symbols enc/2, dec/2;

private a, b, k, ok;

var X, Y, Z;

rewrite dec(enc(X, Y), Y) -> X;

a0 = out(a).out(b).0;

a1 = out(b).out(a).0;

b0 = out(b).out(a).0;

b1 = out(a).out(b).0;

equivalentsq? a0, a1 and b0, b1;
